# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: clinical-metagenomics-1.0
channels:
 - defaults
 - conda-forge
 - bioconda
dependencies:
 - pathoscope=2.0.6
 - metaphlan2=2.7.7
 - fastp=0.20.0
 - multiqc=1.7
 - bwa
 - bbmap=38.75
